<form class="mb-5 mt-2" id="back_delete_button" method="post" action="{{ path('back_category_delete', {'id': category.id}) }}" onsubmit="return confirm('Supprimer cette catégorie?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
