{{ form_start(form) }}

{# Decompose if you want to order the fields in a different order to that of the form. #}
{{ form_row(form.name_category) }}
{{ form_row(form.project) }}

<button class="btn btn-success mb-3">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
