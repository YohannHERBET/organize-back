<!DOCTYPE html>
<html lang="fr">

</html>
<html>

<head>
	<!-- Required meta tags -->
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">

	<!-- Bootstrap CSS -->
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet"
		integrity="sha384-F3w7mX95PdgyTmZZMECAngseQB83DfGTowi0iMjiWaeVhAn4FJkqJByhZMI3AhiU" crossorigin="anonymous">
	<!-- JS close button -->
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js"
		integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous">
	</script>
	<title>
		{% block title %} Administration {% endblock %}
	</title>
	<link rel="icon"
		href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
	<!-- Bootstrap icons -->
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
	<!-- CSS back-office -->
	<link rel="stylesheet" href="{{ asset('css/style.css') }}"> {% block css %}{% endblock %}
</head>

<body>

	<header>

		<nav class="navbar navbar-expand-lg navbar-light bg-light">
			<div class="container-fluid">
				<a class="navbar-brand text-uppercase" href="{{ path('back_home') }}">O'rganize</a>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse"
					data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
					aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarSupportedContent">
					{# securité supplementaire : les liens vers le back sont cachés, si l'utilisateur n'est pas admin #}
					{# {% if is_granted('ROLE_ADMIN') %} #}
					<ul class="navbar-nav me-auto mb-2 mb-lg-0">
						<li class="nav-item">
							<a class="nav-link active opacity-50" aria-current="page"
								href="{{ path('back_user_index') }}">Utilisateurs</a>
						</li>
						<li class="nav-item">
							<a class="nav-link active opacity-50" aria-current="page"
								href="{{ path('back_project_index') }}">Projets</a>
						</li>
						<li class="nav-item">
							<a class="nav-link active opacity-50" aria-current="page"
								href="{{ path('back_category_index') }}">Catégories</a>
						</li>
						<li class="nav-item">
							<a class="nav-link active opacity-50" aria-current="page"
								href="{{ path('back_task_index') }}">Tâches</a>
						</li>
					</ul>

					<div class="container">
						<img id="l-navbar"
							src="{{asset ('img/logo.png')}}"
							alt="logo-back">
					</div>

					<h1 id="title-navback">Administration</h1>
					{# {% endif %} #}
				</div>
			</div>
		</nav>

	</header>

	<div class="container">

		{% block main %}
		<div class="d-flex justify-content-center text-center mt-5">
			<div class="card border-info text-dark bg-light mb-3">
				<div class="card-header text-center">
					<h2>Bienvenue sur l'API d'Organize !</h2>
				</div>
				<div class="card-body">
					<h3 class="card-title">Pour vous connecter a l'API :</h3>
					<p class="card-text">Vous devez tout d'abord vous connecter a l'API en allant sur l'adresse	"/api/login_check" avec votre adresse mail et votre mot de passe</p>
				</div>
				<div class="card-body">
					<h5 class="card-title">Les Endpoints de l'API "Les Utilisateurs" :</h5>
					<p class="card-text">
						<ul>
							<li>
								api/user/{id}: permet de récupérer un utilisateur selon son id (GET)
							</li>
							<li>
								api/user: vous permet de créer un nouvel utilisateur (POST)
							</li>
							<li>
								api/user/{id}: vous permet de modifier un utilisateur (PUT)
							</li>
							<li>
								api/user: vous permet de supprimer votre compte (DELETE)
							</li>
						</ul>
					</p>
				</div>
				<div class="card-body">
					<h5 class="card-title">Les Endpoints de l'API "Les Projets" :</h5>
					<p class="card-text">
						<ul>
							<li>
								api/project: vous permet de récupérer tous les projets (d'un user connecté) (GET)
							</li>
							<li>
								api/project/{id}: permet de récupérer un projet selon son id (GET)
							</li>
							<li>
								api/project: créer un nouveau projet pour un user donné (POST)
							</li>
							<li>
								api/project/{id}: permet de modifier un projet selon son id (PUT)
							</li>
							<li>
								api/project/{id}: permet de supprimer un projet selon son id (DELETE)
							</li>
						</ul>
					</p>
				</div>
				<div class="card-body">
					<h5 class="card-title">Les Endpoints de l'API "Les Catégories":</h5>
					<p class="card-text"></p>
					<ul>
						<li>
							api/category/{id}: permet de récupérer une categorie selon son id (GET)
						</li>
						<li>
							api/category: créer une nouvelle categorie pour un projet donné (POST)
						</li>
						<li>
							api/category/{id}: permet de modifier une categorie selon son id (PUT)
						</li>
						<li>
							api/category/{id}: permet de supprimer une categorie selon son id (DELETE)
						</li>
					</ul>
				</div>
				<div class="card-body">
					<h5 class="card-title">Les Endpoints de l'API "Les taches":</h5>
					<p class="card-text"></p>
					<ul>
						<li>
							api/task: permet de récupérer les 5 prochaines tâches (tâches à venir) pour l'utilisateur connecté (GET)
						</li>
						<li>
							api/task/{id}: permet de récupérer une tâche selon son id (GET)
						</li>
						<li>
							api/task: créer une nouvelle tâche pour une categorie donnée (POST)
						</li>
						<li>
							api/task/{id}: permet de modifier une tâche selon son id (PUT)
						</li>
						<li>
							api/task/{id}: permet de supprimer une tâche selon son id (DELETE)
						</li>
					</ul>
				</div>
			</div>
		</div>

		{% endblock %}

	</div>

</body>

</html>
